info:
  name: delete one
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{HOST}}/v1/{{ROUTE}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

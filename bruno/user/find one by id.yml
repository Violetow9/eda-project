info:
  name: find one by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{HOST}}/v1/{{ROUTE}}/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

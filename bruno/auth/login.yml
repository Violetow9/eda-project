info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{HOST}}/v1/{{ROUTE}}/login"
  body:
    type: json
    data: |-
      {
        "email": "test@gmail.com",
        "password": "maman12345"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
